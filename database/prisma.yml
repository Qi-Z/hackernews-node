# The service name, this will be part of the endpoint of Prisma API. Something like: https://us1.prisma.sh/public-abalonedancer-518/hackernews-node/dev
service: hackernews-node

# The deployment stage, this will also be part of the Prisma API
stage: dev

# Points to the file that holds the data model
datamodel: datamodel.graphql

# You can only access the API when providing with JWT signed with this secret
secret: mysecret123
cluster: public-abalonedancer-518/prisma-us1